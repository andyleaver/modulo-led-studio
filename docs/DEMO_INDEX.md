# Demo Index (fixtures in `V53_work/demos/`)

This list is intentionally limited to fixtures that exist in this build.

## Matrix / HUB75

- **`demo_red_hat_runner_rules_v6.json`** — Stateful runner where **Rules V6** triggers a jump on minute change.
- **`demo_hub75_tilemap_runner_rules_v6.json`** — Tilemap + sprites with Rules V6 driving behaviour (minute-change jump).
- **`demo_hub75_tilemap_runner.json`** — Baseline tilemap runner (scrolling).
- **`demo_hub75_tilemap_runner_2x2.json`** — Multi-panel mapping example (2×2).
- **`demo_hub75_tilemap_runner_clock.json`** — Clock-driven trigger (minute jump) without full rules showcase.
- **`demo_hub75_wifi_ntp_sanity.json`** — WiFi + NTP connectivity sanity.
- **`demo_hub75_wifi_ntp_clockdot.json`** — NTP time drives a clock-dot overlay.
- **`demo_hub75_hw_validation_clockdot.json`** — Minimal validation: solid fill + clock dot.
- **`demo_hub75_hw_mapping_diagnostics.json`** — Corner markers + gradients to validate mapping/orientation.
