"""Backward-compatible purpose channel contract package.

The canonical implementation lives in `params.purpose_contract`.
This package exists so older imports like `from purpose.contract import ...`
keep working.
"""
